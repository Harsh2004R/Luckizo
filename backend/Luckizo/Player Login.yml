info:
  name: Player Login
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8001/api/v1/player/login
  body:
    type: json
    data: |-
      {
        "email":"harshsharmaktm03@gmail.com",
        "password":"Harsh@8080"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
